{
  "name": "dsh-plugins",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "test": "vitest run",
    "pack:check": "node scripts/verify-pack.mjs",
    "check": "pnpm test && pnpm pack:check"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
